# OpenClaw 技能自动触发规则

> 更新：2026-03-25 | 技能自动适配配置

---

## 一、任务类型 → 技能映射

### 1. 数据查询
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 查询客流/票务数据 | `query_data_v2.py` 脚本 |
| 月度/年度统计 | `excel-xlsx` + Python |
| 导出Excel | `excel-xlsx` |

### 2. 竞品监控
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 抖音数据 | `douyin_index_v9.py` |
| 小红书数据 | `xiaohongshu_report.py` |
| 抓取竞品网站 | `scrapling` |
| 爬取网页内容 | `scrapling` |

### 3. 文档处理
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 转Word/PDF | `markdown-converter` / `feishu-doc` |
| 读取PDF | `document-reader` / `feishu-pdf-reader` |
| 飞书文档操作 | `feishu-doc-manager` |

### 4. 报告生成
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 生成日报 | 自动调用 `douyin_index_v9.py` / `xiaohongshu_report.py` |
| 生成周报 | 自动调用对应脚本 |
| 数据分析 | `csv-pipeline` + Python |

### 5. 网络请求
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 测试API | `api-tester` |
| 抓取动态网页 | `scrapling fetch` |
| 绕过反爬 | `scrapling stealthy-fetch` |

### 6. 安全审计
| 用户任务 | 自动使用技能 |
|----------|----------------|
| 检查代码安全 | `audit-code` |
| 扫描敏感信息 | `audit-code` |

---

## 二、自动触发关键词

### 数据类
- "查询" / "统计" / "分析" / "导出"
- "客流" / "营收" / "门票" / "数据"

### 竞品类
- "抖音" / "小红书" / "热榜"
- "竞品" / "只有河南" / "银基" / "万岁山"

### 文档类
- "转Word" / "转PDF" / "转换"
- "读取" / "提取" / "解析"

### 报告类
- "日报" / "周报" / "月报"
- "生成报告" / "数据分析"

### 网络类
- "抓取" / "爬取" / "采集"
- "测试API" / "请求"

---

## 三、工作流程示例

### 示例1：用户说"查一下上周客流"
```
自动识别 → 数据查询 → query_data_v2.py → 返回结果
```

### 示例2：用户说"抓取竞品抖音数据"
```
自动识别 → 竞品监控 → douyin_index_v9.py → 返回数据
```

### 示例3：用户说"把这个PDF转成Word"
```
自动识别 → 文档处理 → markdown-converter → 完成转换
```

### 示例4：用户说"生成今日小红书日报"
```
自动识别 → 报告生成 → xiaohongshu_report.py --daily → 发送报告
```

---

## 四、配置说明

这些规则已内置在系统中。当你提出相关任务时，我会自动查找并使用合适的技能/脚本，无需手动输入命令。

**当前已加载技能**：34个
**Python环境**：~/.scrapling-venv/ (用于scrapling)
